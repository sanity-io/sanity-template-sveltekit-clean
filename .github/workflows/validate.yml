name: Validate Template
on: push

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Validate Sanity Template
        uses: sanity-io/template-validator@v3.1.0
        with:
          repository: ${{ github.repository }}
